var numbers = ['a', 'b', 'c', 'd', 'e',];

number = numbers.indexOf('e') !== -1
numbers[0] = 'sopnil' // get the string of the first position.
numbers.pop() // remove last position of an array
numbers.push('joy')
numbers.shift() // remove the first position of an array
console.log(numbers);


